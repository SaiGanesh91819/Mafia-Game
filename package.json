{
  "name": "mafia-lan-game",
  "version": "1.0.0",
  "description": "Mafia Game Deployment Root",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server",
    "start": "node server/index.js"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {}
}
